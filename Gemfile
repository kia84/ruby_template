# -*- encoding : utf-8 -*-

source 'https://rubygems.org'

gem 'eventmachine'

gem 'bindata'

group :db do
  # Need libpq-dev! `sudo apt-get install libpq-dev`
  gem 'pg', '>= 0.11.0'
end

group :development do
  gem 'capistrano'
  gem 'capistrano-ext'
  gem 'rvm-capistrano'
end
